server:
  port: 8080
spring:
  profile: test
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/userdb
    username: userapp
    password: 12111955DMC88miles
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000 # 1h en millisecondes
# --- Dotenv ---
dotenv:
  enabled: true